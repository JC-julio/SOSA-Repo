import Manager from '../../src/entity/Exits';
import mongoose from 'mongoose';
import { config } from 'dotenv';
config();

test('Testar a classe de novas saidas do SOSA', () => {
  const input = {
    NameStudent: 'Julio',
    NameWorker: 'Ana',
    time: '15',
    observes: 'tudo certo',
  };
});
